#!/usr/bin/env bash
# Скрипт для быстрого запуска приложения с использованием uv

set -e

# Проверка наличия .env файла
if [ ! -f .env ]; then
    echo "⚠️  Файл .env не найден. Копирую из .env.example..."
    cp .env.example .env
    echo "✅ Файл .env создан. Пожалуйста, отредактируйте его перед запуском."
    exit 1
fi

# Проверка наличия uv
if ! command -v uv &> /dev/null; then
    echo "⚠️  uv не найден. Запустите ./setup.sh для установки."
    exit 1
fi

# Запуск приложения
echo "🚀 Запуск VPN Management Service..."
uv run python main.py

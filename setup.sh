#!/usr/bin/env bash
# Скрипт для установки зависимостей с использованием uv

set -e

echo "📦 Установка зависимостей с помощью uv..."

# Проверка наличия uv
if ! command -v uv &> /dev/null; then
    echo "⚠️  uv не найден. Устанавливаю uv..."
    curl -LsSf https://astral.sh/uv/install.sh | sh
    echo "✅ uv установлен!"
fi

# Синхронизация зависимостей (создаст .venv автоматически)
echo "� Синхронизация зависимостей..."
uv sync

echo "✅ Установка завершена!"
echo ""
echo "Для активации виртуального окружения выполните:"
echo "  source .venv/bin/activate"
echo ""
echo "Для запуска приложения выполните:"
echo "  ./run.sh"
echo ""
echo "Полезные команды с uv:"
echo "  uv sync              # Синхронизация зависимостей"
echo "  uv add <package>     # Добавить зависимость"
echo "  uv remove <package>  # Удалить зависимость"
echo "  uv run <command>     # Запустить команду в venv"
